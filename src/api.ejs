---
pagination:
  data: collections.works
  size: 1
  alias: work
permalink: "api/works/<%= work.fileSlug %>.json"
---
<%- 
const {id,title,categories,w,h,location,note,file,year,year_start,year_end,image} = work.data,
JSON.stringify({id,title,categories,w,h,location,note,file,year,year_start,year_end,image}) 
%>
